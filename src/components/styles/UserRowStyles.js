import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  divActions: {
    display: 'flex',
    justifyContent: 'flex-end'
  },
  divider: {
    margin: '0 6px'
  }
}))
